apiVersion: apps/v1
kind: Deployment
metadata:
  name: directory-website
  labels:
    app: directory-website
spec:
  replicas: 1
  selector:
    matchLabels:
      app: directory-website
  template:
    metadata:
      labels:
        app: directory-website
    spec:
      containers:
        - name: directory-website
          image: IMAGE_NAME_PLACEHOLDER
          ports:
            - containerPort: 3000
